const parte1 = ['cabeça', 'ombro', 'joelho'];
const parte2 = ['e' , 'pés'];

const musicaCompleta = [...parte1, ...parte2];
console.log(musicaCompleta);

function fn(x, y, z) {};
var args = [0,1,2];

fn(...args);
